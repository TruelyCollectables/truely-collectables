# Discovery resolved-card removal

Purchase and reject actions now remove the card from the active Discovery queue immediately, revalidate the server route, and force a fresh queue when the browser restores a cached page. Resolved cards are not shown in the active review experience.
