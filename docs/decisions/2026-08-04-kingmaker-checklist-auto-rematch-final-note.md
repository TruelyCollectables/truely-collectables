The database trigger is the authoritative automation boundary. Application code may invoke the same RPC manually for repair or backfill, but ordinary checklist activation requires no operator action.
