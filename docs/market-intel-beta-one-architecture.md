# Architecture Summary

The first Beta One slice uses existing Next.js admin protection and the existing server-only Supabase service-role client. The purchase ledger reads `tcos_mi_purchase_lots`, `tcos_mi_purchase_performance`, and `tcos_mi_inventory_sales`.
