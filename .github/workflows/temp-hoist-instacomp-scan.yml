name: Temporary InstaComp Scan Hoist

on:
  push:
    branches:
      - agent/instacomp-pending-review-overhaul-20260726
    paths:
      - scripts/temp-hoist-instacomp-scan.py
      - .github/workflows/temp-hoist-instacomp-scan.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/instacomp-pending-review-overhaul-20260726
          fetch-depth: 0
      - run: python scripts/temp-hoist-instacomp-scan.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if ! git diff --cached --quiet; then
            git commit -m "Hoist pending scan function for stable hooks"
            git push origin HEAD:agent/instacomp-pending-review-overhaul-20260726
          fi
