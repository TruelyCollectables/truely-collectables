name: Recover Supabase Server Key and Send Admin Reset 20260729

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/authorized-admin-password-recovery-final-deploy-20260729.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: recover-supabase-server-key-admin-reset
  cancel-in-progress: true

jobs:
  recover-deploy-reset:
    if: github.event_name == 'workflow_dispatch' || (github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.head.ref == 'deploy/admin-password-recovery-proxy-20260729')
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
      VERCEL_SCOPE: truelycollectables-projects
      GH_SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SOURCE_SHA: c4dd23b29f50a3fdbd1231bd30e0029c50b969e1
    steps:
      - name: Check out exact merged admin recovery source
        uses: actions/checkout@v4
        with:
          ref: c4dd23b29f50a3fdbd1231bd30e0029c50b969e1
          fetch-depth: 1

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Link exact Vercel project
        run: |
          set -euo pipefail
          test -n "${VERCEL_TOKEN:-}"
          test -n "${GH_SUPABASE_ACCESS_TOKEN:-}"
          echo "::add-mask::$GH_SUPABASE_ACCESS_TOKEN"
          mkdir -p .vercel .deployment-evidence
          npx vercel@56.2.0 whoami --token "$VERCEL_TOKEN"
          if [ -n "${VERCEL_ORG_ID:-}" ] && [ -n "${VERCEL_PROJECT_ID:-}" ]; then
            printf '{"orgId":"%s","projectId":"%s"}\n' "$VERCEL_ORG_ID" "$VERCEL_PROJECT_ID" > .vercel/project.json
          else
            npx vercel@56.2.0 link \
              --yes \
              --project truely-collectables \
              --scope "$VERCEL_SCOPE" \
              --token "$VERCEL_TOKEN"
          fi

      - name: Recover and validate a real Supabase server key
        run: |
          set -euo pipefail
          cat > /tmp/recover-supabase-server-key.mjs <<'NODE'
          import { appendFile } from "node:fs/promises";

          const supabaseUrl = String(process.env.NEXT_PUBLIC_SUPABASE_URL || "").replace(/\/+$/, "");
          const accessToken = String(process.env.GH_SUPABASE_ACCESS_TOKEN || "");
          const githubEnv = String(process.env.GITHUB_ENV || "");
          const storeId = "00000000-0000-4000-8000-000000000001";
          if (!supabaseUrl || !accessToken || !githubEnv) {
            throw new Error("Supabase URL, management access token, or GITHUB_ENV is unavailable.");
          }
          const hostname = new URL(supabaseUrl).hostname;
          const ref = hostname.split(".")[0];
          if (!ref) throw new Error("Supabase project ref could not be derived from the Production URL.");

          const managementHeaders = {
            Authorization: `Bearer ${accessToken}`,
            "Content-Type": "application/json",
          };

          async function listKeys() {
            const response = await fetch(`https://api.supabase.com/v1/projects/${ref}/api-keys?reveal=true`, {
              headers: managementHeaders,
            });
            const text = await response.text();
            if (!response.ok) {
              throw new Error(`Supabase Management API key listing failed: HTTP ${response.status}; ${text.slice(0, 500)}`);
            }
            const keys = JSON.parse(text || "[]");
            for (const entry of keys) {
              if (entry?.api_key) console.log(`::add-mask::${entry.api_key}`);
            }
            console.log(JSON.stringify({
              projectRefResolved: true,
              keyCount: keys.length,
              keys: keys.map((entry) => ({
                idPresent: Boolean(entry?.id),
                type: entry?.type || null,
                name: entry?.name || null,
                prefix: entry?.prefix || null,
                templateRole: entry?.secret_jwt_template?.role || null,
                revealed: Boolean(entry?.api_key),
              })),
            }));
            return keys;
          }

          async function canWrite(key) {
            if (!key) return false;
            console.log(`::add-mask::${key}`);
            const headers = {
              apikey: key,
              Authorization: `Bearer ${key}`,
              "Content-Type": "application/json",
            };
            const read = await fetch(`${supabaseUrl}/rest/v1/store_settings?store_id=eq.${storeId}&select=store_id,metadata`, { headers });
            const readText = await read.text();
            if (!read.ok) return false;
            const rows = JSON.parse(readText || "[]");
            if (rows.length !== 1) return false;
            const metadata = rows[0]?.metadata && typeof rows[0].metadata === "object" ? rows[0].metadata : {};
            const write = await fetch(`${supabaseUrl}/rest/v1/store_settings?store_id=eq.${storeId}`, {
              method: "PATCH",
              headers: { ...headers, Prefer: "return=minimal" },
              body: JSON.stringify({ metadata }),
            });
            return write.ok;
          }

          let keys = await listKeys();
          const preferred = [...keys].sort((left, right) => {
            const score = (entry) => {
              const type = String(entry?.type || "").toLowerCase();
              const name = String(entry?.name || "").toLowerCase();
              const prefix = String(entry?.prefix || "").toLowerCase();
              const role = String(entry?.secret_jwt_template?.role || "").toLowerCase();
              if (type === "secret" || prefix.startsWith("sb_secret_")) return 100;
              if (name.includes("service_role") || name.includes("service role") || role === "service_role") return 90;
              if (type === "legacy") return 20;
              return 0;
            };
            return score(right) - score(left);
          });

          let selected = null;
          for (const entry of preferred) {
            if (await canWrite(entry?.api_key)) {
              selected = entry.api_key;
              console.log(JSON.stringify({ selectedExistingKey: true, selectedType: entry?.type || null, selectedName: entry?.name || null }));
              break;
            }
          }

          if (!selected) {
            const create = await fetch(`https://api.supabase.com/v1/projects/${ref}/api-keys?reveal=true`, {
              method: "POST",
              headers: managementHeaders,
              body: JSON.stringify({
                type: "secret",
                name: "TCOS Production Server",
                description: "Server-only key for Truely Collectables Production",
              }),
            });
            const createText = await create.text();
            if (!create.ok) {
              throw new Error(`No existing key could write and secret-key creation failed: HTTP ${create.status}; ${createText.slice(0, 500)}`);
            }
            const created = JSON.parse(createText || "{}");
            if (created?.api_key) console.log(`::add-mask::${created.api_key}`);
            if (!(await canWrite(created?.api_key))) {
              throw new Error("New Supabase secret key was created but could not write store_settings.");
            }
            selected = created.api_key;
            console.log(JSON.stringify({ createdNewSecretKey: true, selectedType: created?.type || null, selectedName: created?.name || null }));
          }

          console.log(`::add-mask::${selected}`);
          await appendFile(githubEnv, `RECOVERED_SUPABASE_SERVER_KEY=${selected}\n`, "utf8");
          console.log("SUPABASE_SERVER_KEY_RECOVERED_AND_WRITE_VERIFIED");
          NODE
          GH_SUPABASE_ACCESS_TOKEN="$GH_SUPABASE_ACCESS_TOKEN" \
            npx vercel@56.2.0 env run -e production -- node /tmp/recover-supabase-server-key.mjs

      - name: Install validated server key in Vercel Production
        run: |
          set -euo pipefail
          test -n "${RECOVERED_SUPABASE_SERVER_KEY:-}"
          echo "::add-mask::$RECOVERED_SUPABASE_SERVER_KEY"
          printf '%s\n' "$RECOVERED_SUPABASE_SERVER_KEY" | npx vercel@56.2.0 env add SUPABASE_SERVICE_ROLE_KEY production \
            --force \
            --sensitive \
            --scope "$VERCEL_SCOPE" \
            --token "$VERCEL_TOKEN"
          printf '%s\n' 'Supabase server key recovered through the Management API, write-validated, and installed as a sensitive Vercel Production variable.' > .deployment-evidence/supabase-key-repair.txt

      - name: Redeploy exact merged admin recovery source
        id: deploy
        run: |
          set -euo pipefail
          printf '%s\n' "$SOURCE_SHA" > .deployment-evidence/source-sha.txt
          npx vercel@56.2.0 deploy \
            --prod \
            --yes \
            --force \
            --scope "$VERCEL_SCOPE" \
            --token "$VERCEL_TOKEN" \
            2>&1 | tee .deployment-evidence/vercel-deploy.log
          DEPLOYMENT_URL="$(grep -Eo 'https://[^[:space:]]+\.vercel\.app' .deployment-evidence/vercel-deploy.log | tail -n 1)"
          test -n "$DEPLOYMENT_URL"
          echo "deployment_url=$DEPLOYMENT_URL" >> "$GITHUB_OUTPUT"
          printf '%s\n' "$DEPLOYMENT_URL" > .deployment-evidence/deployment-url.txt

      - name: Wait for custom-domain promotion
        run: |
          set -euo pipefail
          ready=""
          for attempt in $(seq 1 30); do
            code="$(curl --location --silent --show-error --connect-timeout 15 --max-time 60 --output .deployment-evidence/custom-login.html --write-out '%{http_code}' https://truelycollectables.com/admin/login || true)"
            if [ "$code" = "200" ] && grep -Fq "Email Owner Reset Link" .deployment-evidence/custom-login.html; then
              ready=1
              break
            fi
            sleep 10
          done
          test -n "$ready"

      - name: Send exactly one custom-domain owner reset email
        run: |
          set -euo pipefail
          code="$(curl --silent --show-error --connect-timeout 15 --max-time 90 \
            --output .deployment-evidence/reset-body.html \
            --dump-header .deployment-evidence/reset.headers \
            --write-out '%{http_code}' \
            --request POST \
            --header 'Content-Type: application/x-www-form-urlencoded' \
            --data-urlencode 'next=/api/admin/market-intel/profit-hunter-token?reveal=1' \
            https://truelycollectables.com/api/admin/password-reset/request || true)"
          location="$(awk 'BEGIN{IGNORECASE=1} /^location:/{sub(/\r$/, ""); print substr($0, 11)}' .deployment-evidence/reset.headers | tail -n 1)"
          echo "reset-request HTTP=$code location=$location"
          case "$code" in 302|303|307|308) ;; *) exit 1 ;; esac
          printf '%s' "$location" | grep -Fq 'reset=sent'
          if printf '%s' "$location" | grep -Eq 'reset=(storage_error|email_error)'; then
            echo "Owner reset email was not accepted."
            exit 1
          fi
          printf '%s\n' 'Production accepted one custom-domain owner reset email request. No password, reset token, management token, or Supabase server key is stored in evidence.' > .deployment-evidence/reset-email-receipt.txt

      - name: Upload redacted evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: recovered-supabase-admin-reset-${{ github.run_number }}
          path: .deployment-evidence
          if-no-files-found: warn
          retention-days: 30
