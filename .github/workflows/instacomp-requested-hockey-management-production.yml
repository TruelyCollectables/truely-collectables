name: InstaComp Requested Hockey Management Production Paused

on:
  push:
    branches:
      - feature/instacomp-target-checklists-20260816
    paths:
      - '.github/workflows/instacomp-requested-hockey-management-production.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: instacomp-requested-hockey-management-production
  cancel-in-progress: true

jobs:
  pause-stuck-writer:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Paused while Supabase database connection layer is restarted.'
