name: InstaComp Registry Consensus Final V4 20260807 Retired

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/instacomp-registry-consensus-final-v4-20260807.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse superseded certification path
        run: |
          echo "Superseded by the visible-set Registry certification. No source or Production mutation is allowed here."
          exit 1
