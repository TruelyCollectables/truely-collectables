name: InstaComp Five Card Production Truth Gate 20260807

on:
  pull_request:
    branches:
      - main
    paths:
      - "scripts/ops/run-instacomp-five-card-production-truth-gate.ts"
      - ".github/workflows/instacomp-five-card-production-truth-gate-20260807.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: instacomp-five-card-production-truth-gate-20260807
  cancel-in-progress: true

env:
  VERCEL_SCOPE: truelycollectables-projects
  VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
  GH_SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
  EVIDENCE_DIR: evidence/instacomp-five-card-production-truth-gate-20260807

jobs:
  prove-five:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ops/instacomp-five-card-production-truth-gate-20260807
          fetch-depth: 1

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install and resolve exact Production credentials
        shell: bash
        run: |
          set -euo pipefail
          npm ci
          test -n "${VERCEL_TOKEN:-}"
          test -n "${GH_SUPABASE_ACCESS_TOKEN:-}"
          echo "::add-mask::$GH_SUPABASE_ACCESS_TOKEN"
          mkdir -p .vercel "$RUNNER_TEMP/instacomp-five-card-truth-gate" "$EVIDENCE_DIR"
          npx vercel@56.2.0 link --yes --project truely-collectables --scope "$VERCEL_SCOPE" --token "$VERCEL_TOKEN" >/dev/null
          npx vercel@56.2.0 env pull "$RUNNER_TEMP/instacomp-five-card-truth-gate/production.env" --yes --environment production --scope "$VERCEL_SCOPE" --token "$VERCEL_TOKEN" >/dev/null

          PRODUCTION_ENV_PATH="$RUNNER_TEMP/instacomp-five-card-truth-gate/production.env" \
          RUNTIME_ENV_PATH="$RUNNER_TEMP/instacomp-five-card-truth-gate/runtime-env.sh" \
          node - <<'NODE'
          const fs = require('node:fs');
          const parsed = {};
          for (const raw of fs.readFileSync(process.env.PRODUCTION_ENV_PATH, 'utf8').split(/\r?\n/)) {
            const line = raw.trim();
            if (!line || line.startsWith('#')) continue;
            const index = line.indexOf('=');
            if (index < 1) continue;
            const key = line.slice(0, index).trim();
            let value = line.slice(index + 1).trim();
            if (value.startsWith('"') && value.endsWith('"')) {
              try { value = JSON.parse(value); } catch { value = value.slice(1, -1); }
            } else if (value.startsWith("'") && value.endsWith("'")) {
              value = value.slice(1, -1);
            }
            parsed[key] = value;
          }

          (async () => {
            const productionUrl = String(parsed.NEXT_PUBLIC_SUPABASE_URL || '');
            if (!productionUrl) throw new Error('Missing NEXT_PUBLIC_SUPABASE_URL');
            const projectRef = new URL(productionUrl).hostname.split('.')[0];
            const response = await fetch(`https://api.supabase.com/v1/projects/${projectRef}/api-keys?reveal=true`, {
              headers: { Authorization: `Bearer ${process.env.GH_SUPABASE_ACCESS_TOKEN}` },
            });
            const body = await response.text();
            if (!response.ok) throw new Error(`Supabase key lookup failed with HTTP ${response.status}: ${body.slice(0, 300)}`);
            const keys = body ? JSON.parse(body) : [];
            if (!Array.isArray(keys)) throw new Error('Supabase key lookup returned a non-array payload');

            const serviceRole =
              keys.find((key) => key?.name === 'service_role' && /^eyJ/.test(String(key?.api_key || ''))) ||
              keys.find((key) => key?.secret_jwt_template?.role === 'service_role' && /^eyJ/.test(String(key?.api_key || '')));
            const anon =
              String(parsed.NEXT_PUBLIC_SUPABASE_ANON_KEY || '') ||
              String(keys.find((key) => key?.name === 'anon' && key?.api_key)?.api_key || '');
            if (!serviceRole?.api_key) throw new Error('Exact legacy service_role JWT was not found');
            if (!anon) throw new Error('Supabase anon key was not found');

            const values = {
              NEXT_PUBLIC_SUPABASE_URL: productionUrl,
              NEXT_PUBLIC_SUPABASE_ANON_KEY: anon,
              SUPABASE_SERVICE_ROLE_KEY: String(serviceRole.api_key),
              GH_SUPABASE_ACCESS_TOKEN: String(process.env.GH_SUPABASE_ACCESS_TOKEN || ''),
            };
            for (const value of Object.values(values)) if (value) console.log(`::add-mask::${value}`);
            fs.writeFileSync(
              process.env.RUNTIME_ENV_PATH,
              Object.entries(values).map(([key, value]) => `export ${key}=${JSON.stringify(value)}`).join('\n') + '\n',
              { mode: 0o600 },
            );
          })().catch((error) => { console.error(error); process.exit(1); });
          NODE

      - name: Compile exact truth gate
        shell: bash
        run: |
          set -euo pipefail
          npx tsc --noEmit

      - name: Run exact five cards through Production scanner
        shell: bash
        run: |
          set -uo pipefail
          source "$RUNNER_TEMP/instacomp-five-card-truth-gate/runtime-env.sh"
          node --import tsx scripts/ops/run-instacomp-five-card-production-truth-gate.ts \
            --output "$EVIDENCE_DIR/receipt.json"
          status=$?
          echo "truth_gate_exit_status=$status"
          test -f "$EVIDENCE_DIR/receipt.json"
          cat "$EVIDENCE_DIR/receipt.json"
          exit "$status"

      - name: Upload exact truth-gate receipt
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: instacomp-five-card-production-truth-gate-20260807
          path: evidence/instacomp-five-card-production-truth-gate-20260807/receipt.json
          if-no-files-found: error
          retention-days: 90

      - name: Remove temporary local credentials
        if: always()
        shell: bash
        run: |
          rm -rf "$RUNNER_TEMP/instacomp-five-card-truth-gate"
          rm -f .env.local .env.production.local
