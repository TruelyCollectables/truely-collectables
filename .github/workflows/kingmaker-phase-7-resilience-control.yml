name: KINGMAKER Phase 7 Resilience Control

on:
  pull_request:
    paths:
      - "src/lib/kingmaker-phase-7-*.ts"
      - "scripts/run-kingmaker-phase-7-*.ts"
      - "supabase/migrations/20260803150000_kingmaker_phase_7_resilience_control.sql"
      - ".github/workflows/kingmaker-phase-7-resilience-control.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: kingmaker-phase-7-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  verify-resilience-control:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Phase 7 resilience regressions
        run: node --import tsx scripts/run-kingmaker-phase-7-resilience-control-regressions.ts
      - name: Migration security contracts
        shell: bash
        run: |
          set -euo pipefail
          migration=supabase/migrations/20260803150000_kingmaker_phase_7_resilience_control.sql
          for table in \
            tcos_kingmaker_service_windows \
            tcos_kingmaker_circuit_states \
            tcos_kingmaker_dead_letters \
            tcos_kingmaker_reconciliations \
            tcos_kingmaker_incidents \
            tcos_kingmaker_release_verdicts
          do
            grep -q "$table" "$migration"
          done
          test "$(grep -c 'enable row level security' "$migration")" -eq 6
          test "$(grep -c 'revoke all' "$migration")" -eq 6
          test "$(grep -c 'grant all' "$migration")" -eq 6
          ! grep -Eiq 'grant .* to (anon|authenticated)' "$migration"
      - name: Diff integrity
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin "+refs/heads/${{ github.base_ref }}:refs/remotes/origin/${{ github.base_ref }}"
          git diff --check "origin/${{ github.base_ref }}...HEAD"
      - name: Lint Phase 7
        run: npx eslint src/lib/kingmaker-phase-7-*.ts scripts/run-kingmaker-phase-7-*.ts
      - name: TypeScript
        run: npx tsc --noEmit
      - name: Production build
        env:
          NEXT_PUBLIC_SUPABASE_URL: https://example.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: build-placeholder
          SUPABASE_SERVICE_ROLE_KEY: build-placeholder
          ADMIN_PASSWORD: build-placeholder
          ADMIN_SESSION_SECRET: build-placeholder-admin-session
          STRIPE_SECRET_KEY: sk_test_build_placeholder
          STRIPE_WEBHOOK_SECRET: whsec_build_placeholder
          EBAY_CLIENT_ID: build-placeholder
          EBAY_CLIENT_SECRET: build-placeholder
          EBAY_ENVIRONMENT: production
          TCOS_LIVE_PAYMENTS_ENABLED: "false"
          TCOS_LIVE_SHIPPING_ENABLED: "false"
          TCOS_SHIPPING_PURCHASE_MODE: dry_run
        run: npm run build
