name: Disaster Recovery Live Infrastructure Snapshot

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Automatic backup-branch live snapshot runs are intentionally disabled after verified capture.'
