The lazy teacher miner is an operational wrapper around run_teacher_vision_lora_training.py.
It preserves the existing teacher/Registry/validation/LoRA contracts while checking cached
receipts before preparing images. Run it with the same arguments as the normal teacher runner.
